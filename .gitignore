node_modules/
*.log
.DS_Store
Thumbs.db
.env

# your workspace map — yours alone, never committed
mithra.config.json
# names the pre-push hook refuses to publish — the list itself is private
.private-names
_cfg.bak
