{
  "//": "Copy to mithra.config.json (gitignored) and edit. Every key is optional.",
  "//both": "One file drives both surfaces: the GUI (npm start) and the MCP server (npm run mcp).",

  "//appName": "GUI only. Window title and the name in the header.",
  "appName": "Mithra",

  "//lang": "GUI only. 'en' or 'es'.",
  "lang": "en",

  "//theme": "GUI only. See ui/public/themes.css for what ships.",
  "theme": "solar",

  "//host": "GUI only. Keep it on 127.0.0.1: the embedded terminal launches a real Claude CLI.",
  "host": "127.0.0.1",
  "port": 7777,

  "//claudeBin": "GUI only. 'auto' looks up claude on PATH; or give an absolute path.",
  "claudeBin": "auto",

  "//feed": "Activity feed. hooks/feed.mjs (registered as a Claude Code hook) appends one line per tool call start/end to `file`; rotated to file.1 past maxBytes. `raw: true` also dumps the hook payloads to file.raw, for debugging only.",
  "//feedLanes": "Rows to colour in the feed panel. tool = name or list, * as wildcard; match = optional regex on the row's summary; slot 1-4 = colour and glyph from the theme. First match wins; everything else is dimmed. label can be a string or { en, es }.",
  "feed": {
    "file": "~/.mithra/feed.jsonl",
    "maxBytes": 5000000,
    "lanes": [
      { "label": "GitHub", "tool": "mcp__github__*", "slot": 1 },
      { "label": "Docker", "tool": ["Bash", "PowerShell"], "match": "\\bdocker\\b", "slot": 2 },
      { "label": { "en": "Subagents", "es": "Subagentes" }, "tool": ["Agent", "Task", "SubagentHandback"], "slot": 3 }
    ]
  },

  "//skills": "GUI only. The chips above the terminal. Each one types its cmd into the prompt.",
  "skills": [
    { "label": "Code Review",   "cmd": "/code-review " },
    { "label": "Deep Research", "cmd": "/deep-research " },
    { "label": "Simplify",      "cmd": "/simplify " },
    { "label": "Run",           "cmd": "/run " }
  ],

  "//root": "Where your projects live — relative to this folder, or absolute. MITHRA_DOCS overrides it.",
  "root": "..",

  "//tasksFile": "Cross-project checklist at the root of `root`. Parsed as ## Status -> ### Project -> - [ ] item.",
  "tasksFile": "TASKS.md",

  "//manualTaskMarkers": "A task containing one of these is flagged as a manual action of yours (not delegable).",
  "manualTaskMarkers": ["(you)"],

  "//docFiles": "GUI only. Per-project docs the Docs tab knows how to render (whitelist).",
  "docFiles": ["CURRENT_FIRE.md", "CLAUDE.md", "AGENTS.md", "README.md"],

  "//workingMemory": "GUI only. A short, hand-curated memory file at the root of `root`, with a size cap.",
  "workingMemoryFile": "WORKING_MEMORY.md",
  "workingMemoryCap": 2000,

  "//vault": "Optional Obsidian vault for Kanban boards, design docs and session notes. Omit or null if you don't use one.",
  "vault": {
    "dir": "MyVault",
    "sessionsDir": "05_Sessions",
    "boardFile": "Board.md"
  },

  "//projects": "'auto' scans `root` for git repos. Or list them explicitly, like below.",
  "//projects2": "vault = this project's folder inside the vault. vaultFilter/vaultExclude narrow it when two projects share one vault folder.",
  "projects": [
    {
      "name": "My App",
      "dir": "my-app",
      "type": "git",
      "deploy": "https://my-app.com",
      "vault": "01_MyApp",
      "priority": 1,
      "sessions": { "include": ["My App"], "exclude": [] },
      "tasks": { "include": ["My App"], "exclude": [] }
    },
    {
      "name": "Landing",
      "dir": "my-landing",
      "type": "fs",
      "deploy": "https://my-app.io",
      "vault": null,
      "priority": 3,
      "board": false,
      "sessions": { "include": ["Landing"], "exclude": [] },
      "tasks": { "include": ["Landing"], "exclude": [] }
    }
  ]
}
